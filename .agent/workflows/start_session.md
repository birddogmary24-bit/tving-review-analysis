---
description: Start session workflow to sync code and check status
---

1. Check current git status
// turbo
2. git pull --rebase origin main
// turbo
3. npm install

Once the pull is complete, inform the user if there were any updates or conflicts. If everything is up to date, let the user know they are ready to code.
